name: Update Steam card
on:
  workflow_dispatch:
  schedule:
    - cron: '23 4 * * *'
permissions:
  contents: write
jobs:
  update:
    uses: mcthesw/steam-stats/.github/workflows/update-card.yml@main
    secrets:
      steam-api-key: ${{ secrets.STEAM_API_KEY }}
